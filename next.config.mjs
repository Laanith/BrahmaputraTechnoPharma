/** @type {import('next').NextConfig} */
const nextConfig = {
    // UNCOMMENT THIS WHEN YOU NEED STATIC HTML FILES AND RUN "npm run build" in terminal
    // output : 'export'
};

export default nextConfig;
